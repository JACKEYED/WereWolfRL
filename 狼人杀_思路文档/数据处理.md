# 数据处理：
## 一、原始发言先做摘要：

降噪 + 压缩上下文:
```text
原始发言：
“我觉得 3 号有点怪，但是 5 号保 3 号也不一定是狼队友……”
摘要后：
“该玩家怀疑 3 号身份偏狼，同时认为 5 号和 3 号可能存在关联。”
```




第 1 局游戏开始
    ↓
本地 Qwen7B 正常参与游戏
    ↓
每一步只记录数据，不训练
    ↓
整局结束，知道胜负
    ↓
计算每一步 reward / return
    ↓
用这一局 trajectory 更新 LoRA
    ↓
得到 Qwen7B-Agent-v1
    ↓
进入第 2 局
    ↓
继续循环













上帝/村长：操作太多了，高级模型只选三个神级操作；

如果这局赢了，就提高这一局中模型输出过的动作概率；
如果这局输了，就降低这一局中模型输出过的动作概率。






每一局的系统数据
每个玩家每次行动的数据，保存成一个turn
每晚投票数据
夜晚狼人行动数据
三类数据：public_state：所有玩家都能看到  private_state：当前 Agent 自己知道的信息  god_state：系统知道，但不能给 Agent
设计一个统一的事件格式
Observation 数据